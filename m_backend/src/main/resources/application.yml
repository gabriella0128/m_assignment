spring:
  datasource:
    #url: jdbc:h2:mem:musinsa
    url: jdbc:h2:tcp://localhost/~/musinsa
    username: sa
    password: pass
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false